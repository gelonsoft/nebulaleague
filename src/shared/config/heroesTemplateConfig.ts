/*import {
    HeroAbilityName, HeroItemId, HeroPosition,
    HeroRanks,
    HeroRanksItems, HeroRoles,
    HeroTemplate, HeroTemplateId
} from "~/shared/models";
import {HeroTemplateSchema} from "~/shared/models/schemas/heroSchemas";

const heroesTemplateConfig: Record<HeroTemplateId, HeroTemplate> = {

}*/